# ts-pattern SWC plugin Vite demo

Create Vite React TypeScript app that installs the local npm package:

```sh
npm install
npm run build
```

`vite.config.ts` imports `@scarf/ts-pattern-swc-plugin/transform` from the installed package and runs it before React's Vite plugin.
