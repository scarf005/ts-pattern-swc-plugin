# Examples

Manually maintained TypeScript examples based on runnable examples from `vendor/ts-pattern/README.md`.

```sh
deno fmt --check
deno task check
```

Each example exports `inputs` and `run` for the web playground.
