# Examples

Manually maintained TypeScript examples for the SWC plugin playground and runtime checks.

```sh
deno task check
```

Each example exports `inputs` and `run` so it can be reused by playground or benchmark code.
